spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: user
              uri: http://localhost:19092/
              predicates:
                - Path=/user/**
              filters:
                - StripPrefix=1
          discovery:
            locator:
              enabled: true

server:
  port: 19091

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
