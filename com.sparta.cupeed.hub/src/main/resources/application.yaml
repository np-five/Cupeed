spring:
  application:
    name: hub

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cupeed-hub
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create  # (none, validate, update, create, create-drop 중 선택)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true  # 콘솔에 SQL 출력 (개발용)
    open-in-view: false
    # 🌟 추가된 설정: JPA DDL(테이블 생성)이 완료될 때까지 데이터소스 초기화(data.sql 실행)를 지연시킵니다.
    defer-datasource-initialization: true

  # 2. SQL 초기화 설정: data.sql 자동 실행
  sql:
    init:
      mode: always

server:
  port: 20040

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

