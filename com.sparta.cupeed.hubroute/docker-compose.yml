services:
  # PostgreSQL 데이터베이스
  postgres:
    image: postgres:15-alpine
    container_name: cupeed-hubroute-postgres
    environment:
      POSTGRES_DB: cupeed-hubroute
      POSTGRES_USER: cupeed-hubroute-user
      POSTGRES_PASSWORD: cupeed-hubroute-pwd
    ports:
      - "15435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cupeed_user -d cupeed_user"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
